import React from 'react';
import { Camera, Moon, Sun, Star, Rocket } from 'lucide-react';

const CreditScoreInfographic = () => {
  return (
    <div 
      className="relative w-[800px] h-[2000px] mx-auto p-6 bg-gradient-to-b from-blue-50 to-blue-100"
      style={{
        backgroundImage: `
          linear-gradient(to bottom, rgba(240,248,255,0.8), rgba(240,248,255,0.8)),
          repeating-linear-gradient(0deg, transparent, transparent 19px, rgba(200,220,255,0.2) 19px, rgba(200,220,255,0.2) 20px)
        `
      }}
    >
      {/* Title Section */}
      <div className="text-center mb-8">
        <h1 className="text-4xl font-bold text-blue-900 mb-2">
          Credit Score Classification Using Machine Learning
        </h1>
        <h2 className="text-xl text-gray-600">
          Random Forest Model
        </h2>
      </div>

      {/* Project Overview */}
      <div className="bg-green-100 rounded-lg p-6 mb-8 shadow-md flex items-center">
        <div className="flex items-center mr-4">
          <Camera className="text-blue-600 mr-2" size={24} />
          <span className="font-semibold">Objective:</span>
        </div>
        <p className="flex-grow">
          Predict credit scores (Low, Average, High) using personal attributes.
        </p>
        <div className="flex items-center ml-4">
          <Moon className="text-blue-600 mr-2" size={24} />
          <span className="font-semibold">Dataset:</span>
          <span className="ml-2">Credit Score Classification Dataset</span>
        </div>
      </div>

      {/* Methodology Flowchart */}
      <div className="flex flex-col items-center mb-8 space-y-4">
        {[
          { label: 'Preprocess Data', color: 'bg-blue-500', Icon: Sun },
          { label: 'Split Data (80% Train, 20% Test)', color: 'bg-orange-500', Icon: Rocket },
          { label: 'Train Random Forest Model', color: 'bg-green-500', Icon: Star },
          { label: 'Tune Hyperparameters', color: 'bg-purple-500', Icon: Camera },
          { label: 'Evaluate & Save Model', color: 'bg-red-500', Icon: Moon }
        ].map((step, index) => (
          <React.Fragment key={step.label}>
            <div className={`flex items-center p-4 rounded-lg shadow-md text-white ${step.color} w-[600px]`}>
              <step.Icon className="mr-4" size={24} />
              <span className="font-semibold">{step.label}</span>
            </div>
            {index < 4 && (
              <div className="h-8 w-1 bg-gray-300"></div>
            )}
          </React.Fragment>
        ))}
      </div>

      {/* Key Results */}
      <div className="flex justify-center mb-8 space-x-8">
        {/* Bar Chart Simulation */}
        <div className="bg-white rounded-lg shadow-md p-6 w-[350px]">
          <h3 className="text-xl font-semibold mb-4 text-center">Initial Model Performance</h3>
          <div className="flex space-x-4 items-end h-40">
            <div className="flex flex-col items-center">
              <div className="bg-orange-500 w-16 h-32"></div>
              <span className="mt-2">Accuracy (85%)</span>
            </div>
            <div className="flex flex-col items-center">
              <div className="bg-orange-500 w-16 h-40"></div>
              <span className="mt-2">ROC-AUC (0.91)</span>
            </div>
          </div>
        </div>

        {/* Parameters Table */}
        <div className="bg-purple-100 rounded-lg shadow-md p-6 w-[350px]">
          <h3 className="text-xl font-semibold mb-4 text-center">Tuned Model Parameters</h3>
          <table className="w-full">
            <tbody>
              <tr>
                <td className="font-semibold">n_estimators:</td>
                <td>200</td>
              </tr>
              <tr>
                <td className="font-semibold">max_depth:</td>
                <td>20</td>
              </tr>
              <tr>
                <td className="font-semibold">min_samples_split:</td>
                <td>2</td>
              </tr>
              <tr>
                <td className="font-semibold">Best Score:</td>
                <td>88%</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      {/* Insights & Improvements */}
      <div className="bg-yellow-100 rounded-lg p-6 mb-8 shadow-md flex items-start">
        <div className="flex items-center mr-4">
          <Star className="text-yellow-600 mr-2" size={24} />
          <span className="font-semibold">Insights:</span>
        </div>
        <p className="flex-grow mr-4">
          Robust model with tuned Random Forest.
        </p>
        <div className="flex items-center">
          <Sun className="text-yellow-600 mr-2" size={24} />
          <span className="font-semibold">Future:</span>
          <span className="ml-2">Add features, test other models</span>
        </div>
      </div>

      {/* Footer */}
      <div className="text-center text-gray-500 flex flex-col items-center">
        <div className="flex items-center mb-2">
          <Camera className="mr-2" size={16} />
          <span>Model saved for deployment - Feb 25, 2025</span>
        </div>
        <div className="text-sm">
          Project Owner: David Osei Kumi
        </div>
      </div>
    </div>
  );
};

export default CreditScoreInfographic;